name: Build & Deploy VSL

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy via rsync
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avz --delete --exclude='.htaccess'
          path: out/
          remote_path: ${{ secrets.DEPLOY_PATH }}/
          remote_host: ${{ secrets.NAPOLEAO_HOST }}
          remote_port: ${{ secrets.NAPOLEAO_PORT }}
          remote_user: ${{ secrets.NAPOLEAO_USER }}
          remote_key: ${{ secrets.NAPOLEAO_SSH_KEY }}
